{
  "name": "alexa-audio",
  "version": "1.0.0",
  "description": "Audio converter/uploader for Amazon Alexa",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "[ -d audio ] || mkdir audio && [ -d audio/src ] || mkdir audio/src && [ -d audio/dist ] || mkdir audio/dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.0",
    "gulp-changed": "^3.2.0",
    "gulp-fluent-ffmpeg": "^1.0.2",
    "gulp-s3-upload": "^1.7.0"
  },
  "devDependencies": {
    "gulp-load-plugins": "^1.5.0"
  }
}
